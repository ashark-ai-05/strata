// Workspace placeholder.
// `tsc --noEmit` returns TS18003 when no .ts files match `include`.
// This file keeps `npm run typecheck` working until per-spike scripts
// exist. Remove once any spike subdirectory contains a real `.ts` file.
export {};
